export type ServiceReview = {
  userPic?: string
  reviewTitle?: string
  userName: string
  userPost?: string
  userCompany?: string
  reviewBody: string
  rating: string
  created_at: string
  helpedPeopleCount?: string
  videoUrl?: string
}
// this could later change depending on the backend
