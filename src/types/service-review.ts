export type ServiceReview = {
  userPic?: string
  userName: string
  userPost?: string
  reviewBody: string
  rating: string
  timestamp: string
  helpedPeopleCount?: string
  videoUrl?: string
}
// this could later change depending on the backend
