export type ServiceReview = {
    userPic?: string
    userName: string
    userPost?: string
    reviewHead?: string
    reviewBody: string
    rating: string
    timestamp: string
    helpedPeopleCount?: string
}
// this could later change depending on the backend
